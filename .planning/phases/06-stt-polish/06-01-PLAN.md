---
phase: 06-stt-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/xuezh/cli/cli.go
  - internal/xuezh/audio/local_stt.go
  - internal/xuezh/audio/audio.go
autonomous: true
---

<objective>
Add STT model selection via CLI flag and enrich transcript output with confidence scores and word-level timestamps.

Purpose: Complete STT-06 (model selection) and STT-07 (confidence/timestamps) to finish the v1.1 Local STT milestone.
Output: `audio stt --model` flag with resolution chain, ProcessVoice model threading, enriched transcript metadata.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-local-stt-client/04-01-SUMMARY.md
@.planning/phases/05-stt-backend-selection/05-01-SUMMARY.md

@internal/xuezh/audio/local_stt.go
@internal/xuezh/audio/audio.go
@internal/xuezh/cli/cli.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --model flag to audio stt CLI and thread model through ProcessVoice</name>
  <files>internal/xuezh/cli/cli.go, internal/xuezh/audio/local_stt.go, internal/xuezh/audio/audio.go</files>
  <action>
  STT-06: Agent can select transcription model for local STT backend.

  1. In `runAudioStt` (cli.go ~955): Add `--model` flag to the FlagSet. After backend resolution, add model resolution chain: flag > config key `stt_model` > env `XUEZH_AUDIO_STT_MODEL` > default `mlx-community/whisper-large-v3-turbo`. Use a simple inline resolution (same pattern as server start model resolution at line ~721, NOT resolveAudioBackend which is backend-specific with global fallback). Pass resolved model to `audio.LocalSTT(*inPath, resolvedModel)` instead of empty string. For whisper backend, model flag is irrelevant (whisper CLI manages its own model), so only pass model when resolvedBackend is "local".

  2. In `runAudioProcessVoice` (cli.go ~1030): Add STT model resolution (same chain as above). Update the ProcessVoice call to pass sttModel.

  3. Update `ProcessVoice` signature (audio.go ~274): Add `sttModel string` parameter. Pass sttModel to `LocalSTT(normalizedPath, sttModel)` when sttBackend is "local". When sttBackend is "whisper", sttModel is unused (whisper CLI manages its own model).

  4. In `local_stt.go`: Remove the `defaultSTTModel` constant (default now lives in CLI resolution chain). The `LocalSTT` function already handles empty model — update the empty-model fallback to use `mlx-community/whisper-large-v3-turbo` as a hardcoded safety net (in case LocalSTT is called without CLI resolution, e.g., programmatically). Keep the existing `if model == "" { model = ... }` pattern but use a package-level const `DefaultSTTModel` (exported, so CLI could reference it if needed, but CLI uses its own resolution chain).

  Avoid: Don't use resolveAudioBackend for model resolution — that function has backend-specific fallback logic (global backend key). Model resolution is simpler: flag > config > env > default.
  </action>
  <verify>go build ./... succeeds with no errors</verify>
  <done>
  - `audio stt --in path --model mlx-community/whisper-large-v3-turbo` passes model to LocalSTT
  - Model resolution chain: flag > config (stt_model) > env (XUEZH_AUDIO_STT_MODEL) > default
  - ProcessVoice accepts and threads sttModel to LocalSTT
  - SttResult data already includes model field (set in LocalSTT line 291)
  </done>
</task>

<task type="auto">
  <name>Task 2: Enrich transcript with confidence scores and word-level timestamps</name>
  <files>internal/xuezh/audio/audio.go</files>
  <action>
  STT-07: Transcription response includes confidence scores and word-level timestamps when available.

  Update `extractTranscript` (audio.go ~379) to extract additional metadata from the verbose_json response when present:

  1. **Segment-level confidence:** For each segment, include `avg_logprob`, `no_speech_prob`, and `compression_ratio` if they exist in the raw segment map. Use the pattern: `if v, ok := segMap["avg_logprob"]; ok { out["avg_logprob"] = v }`. These are float64 values from Whisper's verbose output.

  2. **Word-level timestamps:** For each segment, if `segMap["words"]` exists as `[]any`, extract each word object's `word` (string), `start` (float64), `end` (float64), and `probability` (float64) fields. Add as `"words"` key in the segment output map. Only include the words array if at least one word was extracted.

  3. **Top-level metadata:** After building the transcript map, also copy `duration` if present in raw (this is already done in LocalSTT but NOT in the whisper path's extractTranscript call in STTAudio). Add duration extraction to extractTranscript itself so both backends benefit.

  Avoid: Don't make any field required — all new fields are "when available" (if not in response, omit from output). Don't change the existing text/segments/language structure. The words/confidence fields are additive.
  </action>
  <verify>go build ./... succeeds with no errors</verify>
  <done>
  - extractTranscript includes avg_logprob, no_speech_prob, compression_ratio per segment when present
  - extractTranscript includes words array per segment when present (word, start, end, probability)
  - extractTranscript includes duration at top level when present
  - Existing text/segments/language output unchanged when metadata absent
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go build ./...` succeeds
- [ ] `go vet ./...` passes
- [ ] No new compilation errors or warnings
- [ ] LocalSTT signature unchanged (model param already exists)
- [ ] ProcessVoice signature updated (new sttModel param)
- [ ] CLI callers of ProcessVoice updated
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- STT-06: Model selectable via --model flag with resolution chain
- STT-07: Transcript includes confidence/word-level data when server provides it
- No breaking changes to existing CLI contract (new flag is optional)
</success_criteria>

<output>
After completion, create `.planning/phases/06-stt-polish/06-01-SUMMARY.md`
</output>
