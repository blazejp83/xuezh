---
phase: 05-stt-backend-selection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [internal/xuezh/cli/cli.go, internal/xuezh/audio/audio.go]
autonomous: true
---

<objective>
Add `audio stt` CLI command with `--backend` flag (local vs whisper) and update ProcessVoice to route STT through the resolved backend instead of hardcoding whisper.

Purpose: Requirements STT-04 (whisper unchanged) and STT-05 (explicit backend selection) — mirrors TTS backend pattern from Phase 3.
Output: Working `audio stt --in <file> --backend local|whisper` command and backend-aware ProcessVoice pipeline.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-polish-integration/03-01-SUMMARY.md
@.planning/phases/04-local-stt-client/04-01-SUMMARY.md

@internal/xuezh/cli/cli.go
@internal/xuezh/audio/audio.go
@internal/xuezh/audio/local_stt.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `audio stt` CLI subcommand with backend selection</name>
  <files>internal/xuezh/cli/cli.go</files>
  <action>
  Mirror the TTS handler pattern (lines 860-950) for STT:

  1. Add `case "stt": return runAudioStt(args[1:])` to `runAudio` switch (line 662, before `case "process-voice"`).

  2. Create `runAudioStt(args []string) int` function:
     - Flags: `--in` (required, input audio path), `--backend` (optional, default "whisper"), `--json` (always true).
     - Resolve backend: `resolveAudioBackend(*backend, "whisper", "XUEZH_AUDIO_STT_BACKEND", "stt_backend")`.
     - Backend validation allowlist: only "local" and "whisper" accepted. Reject others with INVALID_ARGUMENT (same pattern as TTS handler line 878-882).
     - Route: `if resolvedBackend == "local"` → `audio.LocalSTT(*inPath, "")` (empty model = default). Else → `audio.STTAudio(*inPath, resolvedBackend)`.
     - Error handling for `LocalSTTError`: match with `errors.As`, emit BACKEND_FAILED with reason + details (mirror TTS LocalTTSError handling at lines 902-915).
     - Error handling for `process.ToolMissingError` and `process.ProcessFailedError`: same pattern as TTS handler (lines 916-935).
     - Fallback error: INVALID_ARGUMENT.
     - Success: `envelope.OK("audio.stt", result.Data, result.Artifacts, result.Truncated, result.Limits)`.

  Command: `audio stt --in <path> [--backend local|whisper] --json`
  </action>
  <verify>go build ./cmd/xuezh-go/ succeeds with no errors</verify>
  <done>`audio stt` command registered in CLI router, backend resolution chain works (flag > config > env > default "whisper"), LocalSTTError and whisper errors both emit structured envelopes</done>
</task>

<task type="auto">
  <name>Task 2: Update ProcessVoice to route STT through resolved backend</name>
  <files>internal/xuezh/cli/cli.go, internal/xuezh/audio/audio.go</files>
  <action>
  Currently ProcessVoice hardcodes whisper for the "local" backend path (line 290):
  `sttResult, err := STTAudio(normalizedPath, "whisper")`

  Update:

  1. In `audio.go` `ProcessVoice` function: Add `sttBackend` parameter (string). Change the local branch (line 289-301) to route based on sttBackend:
     - If `sttBackend == "local"` → `LocalSTT(normalizedPath, "")` (empty model = default).
     - Else → `STTAudio(normalizedPath, "whisper")` (existing behavior).
     Keep the azure.speech branch unchanged (it has its own pronunciation assessment path).

  2. In `cli.go` `runAudioProcessVoice`: Resolve the STT backend:
     `sttBackend := resolveAudioBackend("", "whisper", "XUEZH_AUDIO_STT_BACKEND", "stt_backend")`
     Pass it to `audio.ProcessVoice(*inPath, *refText, backend, sttBackend)`.

  3. Error handling in `runAudioProcessVoice`: Add `LocalSTTError` matching (between the existing AzureSpeechError and ToolMissingError cases):
     ```
     var localSTTErr audio.LocalSTTError
     if errors.As(err, &localSTTErr) { ... emit BACKEND_FAILED ... }
     ```
     Mirror the LocalTTSError pattern from runAudioTTS.

  This preserves whisper as default STT backend (STT-04) while allowing `stt_backend=local` config to switch ProcessVoice to use the mlx-audio server.
  </action>
  <verify>go build ./cmd/xuezh-go/ succeeds with no errors</verify>
  <done>ProcessVoice accepts sttBackend parameter, routes to LocalSTT when "local" and STTAudio when "whisper", LocalSTTError emits structured envelope in process-voice handler</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go build ./cmd/xuezh-go/` succeeds
- [ ] `go vet ./...` passes
- [ ] `audio stt` appears in CLI routing
- [ ] ProcessVoice signature updated with sttBackend parameter
- [ ] Both whisper and local paths compile correctly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- STT-04: Whisper CLI backend unchanged (default behavior preserved)
- STT-05: Backend selection works via --backend flag, config, and env var
- ProcessVoice pipeline routes STT through resolved backend
</success_criteria>

<output>
After completion, create `.planning/phases/05-stt-backend-selection/05-01-SUMMARY.md`
</output>
