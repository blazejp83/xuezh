---
phase: 03-polish-integration
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [internal/xuezh/cli/cli.go, internal/xuezh/audio/local.go]
autonomous: true
---

<objective>
Add optional --instruct parameter to control speech style when using the local TTS backend.

Purpose: TTS-05 (speech style control via instruct parameter). Qwen3-TTS supports an instruct field for controlling speech characteristics like speed, tone, and emotion. This parameter only applies to the local backend — edge-tts does not support it.
Output: CLI accepts --instruct flag, passes it to LocalTTS, which includes it in the HTTP request body to mlx-audio server.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Plan 03-01 modified these files — read current state:
@internal/xuezh/cli/cli.go
@internal/xuezh/audio/local.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --instruct flag to CLI with backend validation</name>
  <files>internal/xuezh/cli/cli.go</files>
  <action>
  In runAudioTTS:

  1. Add --instruct flag after the existing voice flag:
     `instruct := fs.String("instruct", "", "speech style instruction (local backend only, e.g. 'speak slowly')")`

  2. After backend validation (added in Plan 03-01), validate that instruct is only used with local backend:
     ```
     if *instruct != "" && resolvedBackend != "local" {
         return emitTypedError("audio.tts", "INVALID_ARGUMENT",
             "--instruct is only supported with --backend local",
             map[string]any{"backend": resolvedBackend, "instruct": *instruct})
     }
     ```

  3. Update the LocalTTS call to pass instruct:
     Change: `result, err = audio.LocalTTS(*text, *voice, *outPath, "tts_audio")`
     To:     `result, err = audio.LocalTTS(*text, *voice, *instruct, *outPath, "tts_audio")`

  Do NOT change the edge-tts path. The TTSAudio call signature remains unchanged.
  </action>
  <verify>go build ./... && go vet ./...</verify>
  <done>CLI accepts --instruct flag, validates it's only used with local backend, passes to LocalTTS.</done>
</task>

<task type="auto">
  <name>Task 2: Wire instruct parameter into LocalTTS HTTP body</name>
  <files>internal/xuezh/audio/local.go</files>
  <action>
  In local.go:

  1. Update LocalTTS signature to accept instruct parameter:
     `func LocalTTS(text, voice, instruct, outPath, purpose string) (AudioResult, error) {`

  2. Replace the fmt.Sprintf body construction (line ~215) with json.Marshal for cleaner conditional field handling. Build a map for the request body:
     ```
     reqBody := map[string]string{
         "model": model,
         "input": text,
         "voice": voice,
     }
     if instruct != "" {
         reqBody["instruct"] = instruct
     }
     bodyBytes, err := json.Marshal(reqBody)
     if err != nil {
         return AudioResult{}, fmt.Errorf("failed to marshal TTS request: %w", err)
     }
     ```
     Then use `bytes.NewReader(bodyBytes)` instead of `bytes.NewBufferString(body)` in the client.Post call.

     Add `"encoding/json"` to the import block if not already present.

  3. Include instruct in the response data map (alongside text, voice, out, model, backend, available_voices):
     ```
     if instruct != "" {
         data["instruct"] = instruct
     }
     ```

  Do NOT change any error handling, classification, or the rest of the function.
  </action>
  <verify>go build ./... && go vet ./...</verify>
  <done>LocalTTS accepts instruct param. HTTP body includes instruct when non-empty. Response envelope includes instruct when used.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go build ./...` succeeds
- [ ] `go vet ./...` passes
- [ ] LocalTTS function signature updated correctly
- [ ] --instruct with edge-tts returns INVALID_ARGUMENT
- [ ] --instruct with local backend passes value to HTTP body
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Instruct parameter works with local backend (TTS-05)
- Instruct parameter rejected for edge-tts backend
- No changes to edge-tts code path
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish-integration/03-02-SUMMARY.md`
</output>
