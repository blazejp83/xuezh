---
phase: 03-polish-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [internal/xuezh/cli/cli.go, internal/xuezh/audio/local.go]
autonomous: true
---

<objective>
Make backend selection robust with backend-aware voice defaults, backend validation, and voice discoverability for the local TTS backend.

Purpose: BKND-01 (edge-tts unchanged), BKND-02 (explicit backend selection), TTS-06 (dialect voices discoverable). Currently the --voice flag defaults to "XiaoxiaoNeural" (edge-tts voice) even when --backend local is used, causing confusing results. Backend names are also unvalidated.
Output: CLI correctly defaults voice per backend, rejects invalid backends, and local TTS responses include available_voices metadata.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@internal/xuezh/cli/cli.go
@internal/xuezh/audio/local.go
@internal/xuezh/audio/audio.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend-aware voice default and backend validation</name>
  <files>internal/xuezh/cli/cli.go</files>
  <action>
  In runAudioTTS (around line 861):

  1. Change voice flag default from "XiaoxiaoNeural" to empty string "":
     `voice := fs.String("voice", "", "voice (XiaoxiaoNeural for edge-tts, Vivian for local)")`

  2. After resolveAudioBackend call (line 875), add backend validation. Only "local" and "edge-tts" are valid TTS backends. If resolvedBackend is anything else, return INVALID_ARGUMENT:
     ```
     if resolvedBackend != "local" && resolvedBackend != "edge-tts" {
         return emitTypedError("audio.tts", "INVALID_ARGUMENT",
             fmt.Sprintf("unsupported TTS backend: %s (valid: local, edge-tts)", resolvedBackend),
             map[string]any{"backend": resolvedBackend})
     }
     ```

  3. After validation, apply backend-specific voice default. If voice is empty:
     - For "edge-tts": set voice to "XiaoxiaoNeural"
     - For "local": leave empty (LocalTTS already defaults to "Vivian" internally)
     ```
     if *voice == "" && resolvedBackend != "local" {
         *voice = "XiaoxiaoNeural"
     }
     ```

  Do NOT change any other behavior. The routing logic (local vs edge-tts) and error handling remain exactly as-is.
  </action>
  <verify>go build ./... && go vet ./...</verify>
  <done>Voice flag defaults correctly per backend. Invalid backends return INVALID_ARGUMENT envelope. Edge-tts path behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add available_voices metadata to local TTS response</name>
  <files>internal/xuezh/audio/local.go</files>
  <action>
  In local.go:

  1. Add a package-level variable listing known Qwen3-TTS voices:
     ```
     var localVoices = []map[string]string{
         {"name": "Vivian", "description": "standard Mandarin, female"},
         {"name": "Dylan", "description": "Beijing dialect, male"},
         {"name": "Eric", "description": "Sichuan dialect, male"},
     }
     ```

  2. In the LocalTTS function, add "available_voices" to the response data map (around line 266-276, where the data map is built):
     ```
     data := map[string]any{
         "text":  text,
         "voice": voice,
         "out":   artifact.Path,
         "model": model,
         "backend": map[string]any{
             "id":       "local",
             "features": []string{"tts"},
         },
         "available_voices": localVoices,
     }
     ```

  Do NOT add client-side voice validation — let the mlx-audio server validate voice names and return errors naturally. The available_voices field is for agent discoverability only.
  </action>
  <verify>go build ./... && go vet ./...</verify>
  <done>Local TTS response includes available_voices list with Vivian, Dylan, Eric. Agents can discover voice options from response metadata.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go build ./...` succeeds
- [ ] `go vet ./...` passes
- [ ] No changes to edge-tts code path (TTSAudio function unchanged)
- [ ] runAudioTTS handles empty --voice correctly for both backends
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Edge-tts backward compatibility preserved (BKND-01)
- Backend validation rejects invalid backends (BKND-02)
- Dialect voices discoverable via available_voices (TTS-06)
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish-integration/03-01-SUMMARY.md`
</output>
